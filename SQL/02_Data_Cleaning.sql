SELECT *
INTO Placements_Backup
FROM Placements;

SELECT COUNT(*) FROM Placements;

SELECT DISTINCT Sales_Manager_BDM
FROM Placements;

SELECT DISTINCT Type
FROM Placements;

SELECT TOP 20 *
FROM Placements;